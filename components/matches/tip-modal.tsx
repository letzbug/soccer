'use client'; import * as Dialog from '@radix-ui/react-dialog'; import { useState } from 'react'; import { Match } from '@/data/matches'; import { Button } from '@/components/ui/button'; import { useTipStore } from '@/store/tip-store';
export function TipModal({match,children}:{match:Match;children:React.ReactNode}){const setTip=useTipStore(s=>s.setTip); const [home,setHome]=useState(0); const [away,setAway]=useState(0); return <Dialog.Root><Dialog.Trigger asChild>{children}</Dialog.Trigger><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-black/70 backdrop-blur-sm"/><Dialog.Content className="glass fixed left-1/2 top-1/2 z-50 w-[92vw] max-w-lg -translate-x-1/2 -translate-y-1/2 rounded-3xl p-6 shadow-glow"><Dialog.Title className="gold-text text-center text-3xl font-black">Dein Tipp</Dialog.Title><div className="mt-6 grid grid-cols-[1fr_auto_1fr] items-center gap-3"><div className="text-center text-5xl">{match.homeFlag}<p className="mt-2 text-base font-bold">{match.home}</p></div><div className="text-2xl font-black text-goldx">VS</div><div className="text-center text-5xl">{match.awayFlag}<p className="mt-2 text-base font-bold">{match.away}</p></div></div><div className="mt-8 flex items-center justify-center gap-4"><input className="input w-20 text-center text-3xl font-black" type="number" min={0} max={20} value={home} onChange={e=>setHome(+e.target.value)}/><span className="text-3xl">:</span><input className="input w-20 text-center text-3xl font-black" type="number" min={0} max={20} value={away} onChange={e=>setAway(+e.target.value)}/></div><div className="mt-8 flex justify-end gap-3"><Dialog.Close asChild><Button variant="ghost">Abbrechen</Button></Dialog.Close><Dialog.Close asChild><Button onClick={()=>setTip(match.id,home,away)}>Tipp speichern</Button></Dialog.Close></div></Dialog.Content></Dialog.Portal></Dialog.Root>}
