import Image from 'next/image'; import { Stadium } from '@/data/stadiums'; import { StadiumModal } from './stadium-modal';
export function StadiumCard({stadium}:{stadium:Stadium}){return <StadiumModal stadium={stadium}><button className="glass card-hover group overflow-hidden rounded-3xl text-left"><div className="relative h-48"><Image src={stadium.image} alt={stadium.name} fill className="object-cover transition group-hover:scale-105"/></div><div className="p-5"><p className="text-sm text-emeraldx">{stadium.city} · {stadium.country}</p><h3 className="text-xl font-black">{stadium.name}</h3><p className="mt-2 text-sm text-white/60">{stadium.capacity.toLocaleString('de-DE')} Plätze · Baujahr {stadium.opened}</p></div></button></StadiumModal>}
